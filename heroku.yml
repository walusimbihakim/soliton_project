build:
  docker:
    web: Dockerfile
    worker: Dockerfile
    beat: Dockerfile
run:
  worker:
    command:
      - celery -A project_manager worker --beat --scheduler django_celery_beat.schedulers:DatabaseScheduler --loglevel=info



